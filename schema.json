{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ForgeConfig",
  "type": "object",
  "properties": {
    "urls": {
      "type": "array",
      "description": "List of Forge endpoints for the language server to use.",
      "items": {
        "type": "string",
        "format": "uri"
      }
    }
  },
  "required": [
    "urls"
  ],
  "additionalProperties": false
}